# Plan 14 — Loki (loggaggregering) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give Devroom log observability: the 5 Spring services log structured JSON, Grafana Alloy collects container logs as a DaemonSet and pushes to Loki, and a provisioned Loki datasource lets logs be queried and correlated in the existing Grafana.

**Architecture:** Enable Spring Boot 4's built-in structured (ECS-JSON) console logging via one property per service. Install Loki (single-binary) + Grafana Alloy (DaemonSet) as their own Helm releases in `monitoring`. A Loki datasource ConfigMap is provisioned into Grafana, and a logs panel is added to the existing dashboard.

**Tech Stack:** Spring Boot structured logging, Grafana Loki (chart 7.0.0, app 3.6.7), Grafana Alloy (chart 1.10.0, app v1.17.0), Grafana (from Plan 13).

**Spec:** `docs/superpowers/specs/2026-06-24-plan-14-loki-design.md`

**Branch:** `plan-14-loki` (already created; spec already committed).

**Prerequisites:** Plans 11–13 merged. `helm`, `kubectl`, `minikube`, `docker` installed. The monitoring stack (Plan 13) provides Grafana with a datasource sidecar. Minikube only needed for Task 5.

---

## File Structure

| File | Responsibility |
|---|---|
| `services/*/src/main/resources/application.yml` (×5) | `logging.structured.format.console=ecs` |
| `helm/loki-values.yaml` | Loki chart values (single-binary, filesystem) |
| `helm/alloy-values.yaml` | Alloy config: scrape pod logs → push to Loki |
| `helm/grafana-loki-datasource.yaml` | Grafana datasource ConfigMap (monitoring ns) |
| `helm/install-logging.sh` | Install Loki + Alloy + apply the datasource |
| `helm/devroom/templates/grafana-dashboard.yaml` | Add a logs panel |
| `docs/adr/0013-loki-alloy-logging.md` | ADR |

---

## Task 1: Structured JSON logging in the 5 Spring services

Spring Boot 4 ships built-in structured logging — `logging.structured.format.console=ecs` makes each stdout line an ECS-JSON object. Four services have no top-level `logging:` block (append one); gateway already has `logging.level` (merge into it).

**Files:**
- Modify: `services/{auth,user,message,bot}-service/src/main/resources/application.yml`
- Modify: `services/gateway/src/main/resources/application.yml`

- [ ] **Step 1: Append a `logging` block to auth/user/message/bot application.yml**

At the END of each of `services/{auth,user,message,bot}-service/src/main/resources/application.yml`, append:

```yaml

logging:
  structured:
    format:
      console: ecs
```

- [ ] **Step 2: Merge into gateway's existing `logging` block**

In `services/gateway/src/main/resources/application.yml`, find:

```yaml
logging:
  level:
    org.springframework.cloud.gateway: INFO
    org.springframework.security: INFO
    org.springframework.web: INFO
```

Replace with:

```yaml
logging:
  structured:
    format:
      console: ecs
  level:
    org.springframework.cloud.gateway: INFO
    org.springframework.security: INFO
    org.springframework.web: INFO
```

- [ ] **Step 3: Verify the property is present in all 5**

Run: `grep -rl "structured" services/*/src/main/resources/application.yml | wc -l | tr -d ' '`
Expected: `5`

- [ ] **Step 4: Verify a service still boots with the property**

Run: `mvn -q -pl services/user-service verify 2>&1 | grep -E "BUILD FAILURE|ERROR" | head -3; grep -h "Tests run" services/user-service/target/surefire-reports/*.txt | sed 's/^/  /'`
Expected: no `BUILD FAILURE`; surefire lines show 0 failures/errors (the property doesn't break context startup).

Bonus (may or may not print depending on surefire output capture): `mvn -q -pl services/user-service verify 2>&1 | grep -m1 '"ecs.version"'` — if it prints a JSON line with `"ecs.version"`, ECS-JSON is confirmed offline. The definitive in-cluster JSON check is Task 5 Step 4.

- [ ] **Step 5: Commit**

```bash
git add services/*/src/main/resources/application.yml
git commit -m "feat(plan-14): structured ECS-JSON console logging in the 5 services"
```

---

## Task 2: Install Loki + Alloy + Grafana datasource

Loki (single-binary) stores logs; Alloy (DaemonSet) ships them; a datasource ConfigMap wires Loki into Grafana. All in the `monitoring` namespace.

**Files:**
- Create: `helm/loki-values.yaml`
- Create: `helm/alloy-values.yaml`
- Create: `helm/grafana-loki-datasource.yaml`
- Create: `helm/install-logging.sh`

- [ ] **Step 1: Create `helm/loki-values.yaml`**

```yaml
# Single-binary Loki for Minikube: one pod, filesystem storage, no caches.
deploymentMode: SingleBinary
loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
  schemaConfig:
    configs:
      - from: "2024-04-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: loki_index_
          period: 24h
singleBinary:
  replicas: 1
# Disable the scalable/SimpleScalable components and extras.
read:
  replicas: 0
write:
  replicas: 0
backend:
  replicas: 0
chunksCache:
  enabled: false
resultsCache:
  enabled: false
lokiCanary:
  enabled: false
test:
  enabled: false
gateway:
  enabled: false
```

- [ ] **Step 2: Create `helm/alloy-values.yaml`**

```yaml
controller:
  type: daemonset
alloy:
  mounts:
    varlog: true
  configMap:
    content: |
      discovery.kubernetes "pods" {
        role = "pod"
      }

      discovery.relabel "pod_logs" {
        targets = discovery.kubernetes.pods.targets
        rule {
          source_labels = ["__meta_kubernetes_namespace"]
          target_label  = "namespace"
        }
        rule {
          source_labels = ["__meta_kubernetes_pod_name"]
          target_label  = "pod"
        }
        rule {
          source_labels = ["__meta_kubernetes_pod_container_name"]
          target_label  = "container"
        }
        rule {
          source_labels = ["__meta_kubernetes_pod_label_app"]
          target_label  = "app"
        }
        rule {
          source_labels = ["__meta_kubernetes_pod_uid", "__meta_kubernetes_pod_container_name"]
          separator     = "/"
          action        = "replace"
          replacement   = "/var/log/pods/*$1/*.log"
          target_label  = "__path__"
        }
      }

      local.file_match "pod_logs" {
        path_targets = discovery.relabel.pod_logs.output
      }

      loki.source.file "pod_logs" {
        targets    = local.file_match.pod_logs.targets
        forward_to = [loki.write.default.receiver]
      }

      loki.write "default" {
        endpoint {
          url = "http://loki.monitoring.svc:3100/loki/api/v1/push"
        }
      }
```

- [ ] **Step 3: Create `helm/grafana-loki-datasource.yaml`**

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-datasource
  namespace: monitoring
  labels:
    grafana_datasource: "1"
data:
  loki-datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: Loki
        type: loki
        uid: loki
        access: proxy
        url: http://loki.monitoring.svc:3100
        jsonData:
          maxLines: 1000
```

- [ ] **Step 4: Create `helm/install-logging.sh`**

```bash
#!/usr/bin/env bash
# Installs Loki (single-binary) + Grafana Alloy (DaemonSet) in the monitoring
# namespace, and provisions a Loki datasource into the existing Grafana.
# Run after install-monitoring.sh (Plan 13).
set -euo pipefail
cd "$(dirname "$0")/.."

echo "==> Adding grafana Helm repo"
helm repo add grafana https://grafana.github.io/helm-charts >/dev/null 2>&1 || true
helm repo update grafana >/dev/null

echo "==> Installing Loki (chart 7.0.0, single-binary)"
helm upgrade --install loki grafana/loki \
  --version 7.0.0 -n monitoring --create-namespace \
  -f helm/loki-values.yaml --wait --timeout 10m

echo "==> Installing Grafana Alloy (chart 1.10.0, DaemonSet)"
helm upgrade --install alloy grafana/alloy \
  --version 1.10.0 -n monitoring \
  -f helm/alloy-values.yaml --wait --timeout 5m

echo "==> Provisioning Loki datasource into Grafana"
kubectl apply -f helm/grafana-loki-datasource.yaml

echo "==> Logging stack ready. Query logs in Grafana → Explore → Loki."
kubectl get pods -n monitoring -l 'app.kubernetes.io/name in (loki, alloy)'
```

- [ ] **Step 5: Validate the values against the real charts offline (dry-run render)**

Run: `helm template loki grafana/loki --version 7.0.0 -f helm/loki-values.yaml >/dev/null && echo "loki values OK"`
Expected: `loki values OK` (chart renders with our values — catches schema/typo errors without a cluster).

Run: `helm template alloy grafana/alloy --version 1.10.0 -f helm/alloy-values.yaml >/dev/null && echo "alloy values OK"`
Expected: `alloy values OK`

- [ ] **Step 6: Validate the datasource YAML and script syntax**

Run: `python3 -c "import yaml; yaml.safe_load(open('helm/grafana-loki-datasource.yaml')); print('datasource yaml OK')"`
Expected: `datasource yaml OK`

Run: `chmod +x helm/install-logging.sh && bash -n helm/install-logging.sh && echo "script OK"`
Expected: `script OK`

- [ ] **Step 7: Commit**

```bash
git add helm/loki-values.yaml helm/alloy-values.yaml helm/grafana-loki-datasource.yaml helm/install-logging.sh
git commit -m "feat(plan-14): Loki + Alloy install with Grafana datasource"
```

---

## Task 3: Add a logs panel to the dashboard

**Files:**
- Modify: `helm/devroom/templates/grafana-dashboard.yaml`

- [ ] **Step 1: Add a logs panel to the panels array**

In `helm/devroom/templates/grafana-dashboard.yaml`, find the last panel (HTTP requests) and the closing of the panels array:

```
        {
          "type": "timeseries", "title": "HTTP requests / sec by service",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "targets": [ { "expr": "sum(rate(http_server_requests_seconds_count[5m])) by (job)", "legendFormat": "{{`{{job}}`}}" } ]
        }
      ]
    }
```

Replace with (add a comma after the HTTP panel, then the logs panel):

```
        {
          "type": "timeseries", "title": "HTTP requests / sec by service",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "targets": [ { "expr": "sum(rate(http_server_requests_seconds_count[5m])) by (job)", "legendFormat": "{{`{{job}}`}}" } ]
        },
        {
          "type": "logs", "title": "Logs (devroom)",
          "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 },
          "datasource": { "type": "loki", "uid": "loki" },
          "options": { "showTime": true, "wrapLogMessage": true },
          "targets": [ { "expr": "{namespace=\"devroom\"}", "datasource": { "type": "loki", "uid": "loki" } } ]
        }
      ]
    }
```

- [ ] **Step 2: Verify the dashboard JSON is still valid with 5 panels**

Run: `helm template devroom helm/devroom --show-only templates/grafana-dashboard.yaml | python3 -c "import sys,yaml,json; cm=yaml.safe_load(sys.stdin); d=json.loads(cm['data']['devroom-overview.json']); print(len(d['panels']),'panels;', [p['type'] for p in d['panels']])"`
Expected: `5 panels; ['timeseries', 'timeseries', 'timeseries', 'timeseries', 'logs']`

- [ ] **Step 3: Lint and commit**

```bash
helm lint helm/devroom
git add helm/devroom/templates/grafana-dashboard.yaml
git commit -m "feat(plan-14): add Loki logs panel to the Devroom dashboard"
```

---

## Task 4: ADR-0013

**Files:**
- Create: `docs/adr/0013-loki-alloy-logging.md`

- [ ] **Step 1: Create `docs/adr/0013-loki-alloy-logging.md`**

```markdown
# ADR-0013: Loki + Grafana Alloy + strukturerad JSON-loggning

**Status:** Accepted
**Date:** 2026-06-27

## Context

Fas B fortsätter med loggar. Tjänsterna loggar plain text till stdout, spridda per
pod (bara `kubectl logs`). Vi behöver aggregera loggar och göra dem sökbara,
helst korrelerbara med metrics i samma Grafana.

## Decision

Tjänsterna loggar **strukturerad JSON** (Spring Boot 4:s inbyggda
`logging.structured.format.console=ecs`). **Grafana Alloy** (DaemonSet) samlar
container-loggarna och pushar till **Loki** (single-binary). En Loki-datakälla
provisioneras i den befintliga Grafanan; loggar frågas med LogQL i Explore och
visas i en panel bredvid metrics.

## Considered alternatives

### 1. Promtail som collector
Lokis klassiska agent. Avvisad: deprecat:erad till förmån för Alloy (EOL ~2026).
Alloy är dessutom en enhetlig agent som återanvänds för traces i Plan 15.

### 2. ELK (Elasticsearch + Kibana)
Kraftfullt och vanligt i jobbannonser, men tungt (Elasticsearch-RAM) och ett eget
UI-ekosystem. Avvisat till förmån för Grafana-stacken: en UI för metrics + loggar +
traces. (Kan läggas till separat senare om Kibana-erfarenhet specifikt önskas.)

### 3. Plain-text-loggar
Enklast (ingen app-ändring), men ingen fält-frågning. Avvisat för JSON som låter
Loki filtrera på `log_level` m.m.

## Consequences

- Loki + Alloy som egna releaser i `monitoring`; `install-logging.sh` körs efter
  `install-monitoring.sh` (Plan 13) och före devroom-deployen.
- stdout blir JSON (mindre läsbart lokalt i `docker logs`/`mvn`); priset för
  maskin-frågbara loggar.
- Loki single-binary + filsystem räcker lokalt; objektlagring/retention är
  out-of-scope.
- Tracing (Tempo) i Plan 15 återanvänder Alloy som collector.
```

- [ ] **Step 2: Commit**

```bash
git add docs/adr/0013-loki-alloy-logging.md
git commit -m "docs(plan-14): ADR-0013 Loki + Alloy + structured JSON logging"
```

---

## Task 5: End-to-end on Minikube

Live verification. Requires the Plan 13 monitoring stack. Run logging install after monitoring, then redeploy the app so its JSON logs flow.

**Files:** none (verification only)

- [ ] **Step 1: Ensure cluster + monitoring are up**

Run: `minikube status | grep -q Running || minikube start --driver=docker --memory=8192 --cpus=4`
Then ensure ingress + monitoring exist:
Run: `bash helm/setup-ingress.sh && bash helm/install-monitoring.sh`
Expected: Traefik, CoreDNS, and the kube-prometheus-stack are ready.

- [ ] **Step 2: Install the logging stack**

Run: `bash helm/install-logging.sh`
Expected: Loki pod and Alloy DaemonSet pods Running; datasource applied.

- [ ] **Step 3: Deploy the app (rebuilds images with JSON logging)**

Run: `OPENROUTER_API_KEY="${OPENROUTER_API_KEY:-}" bash helm/deploy.sh`
Expected: all 8 deployments available.

- [ ] **Step 4: Verify a service logs ECS-JSON in the cluster**

Run: `kubectl logs -n devroom deploy/auth-service --tail=20 | grep -m1 '"ecs.version"' | python3 -m json.tool >/dev/null && echo "valid ECS-JSON in cluster"`
Expected: `valid ECS-JSON in cluster` (a log line parses as JSON).

- [ ] **Step 5: Verify Loki received devroom logs**

Run: `kubectl run lokitest --rm --attach --restart=Never --image=curlimages/curl -n monitoring -- curl -sG 'http://loki.monitoring.svc:3100/loki/api/v1/label/namespace/values' | grep -o devroom`
Expected: `devroom` (Loki knows the namespace label → Alloy is shipping logs).

- [ ] **Step 6: View logs in Grafana** (manual)

With `minikube tunnel` running and `grafana.devroom.local` in `/etc/hosts` (Plan 13), open `http://grafana.devroom.local` → Explore → datasource **Loki** → query `{namespace="devroom"} | json | log_level="INFO"`. Confirm logs appear and the "Devroom Overview" dashboard's Logs panel renders.

- [ ] **Step 7: Record the result**

No commit. Note in the PR: services log ECS-JSON, Loki has the `devroom` namespace label, Grafana Explore shows logs.

---

## Task 6: Documentation

**Files:**
- Modify: `CLAUDE.md`
- Modify: `README.md`

- [ ] **Step 1: Append a Plan 14 status block to `CLAUDE.md`** (after the Plan 13 block, same Swedish bullet style)

Cover: `logging.structured.format.console=ecs` on the 5 services (Boot 4 built-in, ECS-JSON); Loki single-binary (chart 7.0.0) + Alloy DaemonSet (chart 1.10.0) as own releases in `monitoring` via `helm/install-logging.sh`; Alloy config (discovery.kubernetes + loki.source.file → loki.write); Loki datasource ConfigMap in `monitoring` (datasource sidecar only searches there); logs panel added to the dashboard; install order (monitoring → logging → deploy); ADR-0013; what was verified (mvn boots with ECS-JSON, helm template of loki/alloy values, dashboard JSON). End with **Nästa steg:** merge + Plan 15 (Tempo/tracing, reuses Alloy).

- [ ] **Step 2: Update `README.md`** — add a "Loggar (Loki)" subsection after the metrics section:

```markdown
### Loggar med Loki

Se [ADR-0013](docs/adr/0013-loki-alloy-logging.md). Tjänsterna loggar strukturerad
ECS-JSON; Grafana Alloy (DaemonSet) samlar containerloggarna och pushar till Loki.
Loggarna frågas i Grafana → Explore (datakälla Loki) och syns i en panel i
"Devroom Overview".

```bash
bash helm/install-monitoring.sh  # Plan 13 (om inte redan kört)
bash helm/install-logging.sh     # Loki + Alloy + Grafana-datakälla
bash helm/deploy.sh              # appen (JSON-loggar + logs-panel)
# Grafana → Explore → Loki:  {namespace="devroom"} | json | log_level="ERROR"
```
```

Add a status table row: `| 14 | Loggar: Loki + Alloy + strukturerad JSON (ADR-0013) | 14 | 2026-06-27 |`.

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md README.md
git commit -m "docs(plan-14): document Loki logging and Plan 14 status"
```

---

## Self-Review Notes

- **Spec coverage:** structured JSON (Task 1), Loki single-binary + Alloy DaemonSet + install script (Task 2), Grafana datasource in monitoring ns (Task 2), logs panel showpiece (Task 3), ADR-0013 (Task 4), live verification + docs (Tasks 5–6). All spec sections map to a task.
- **Datasource placement:** the Loki datasource ConfigMap is in the `monitoring` namespace (applied by install-logging.sh), because Grafana's datasource sidecar only searches there by default (Plan 13 set `searchNamespace=ALL` for dashboards only). The dashboard logs panel stays in the devroom chart (dashboard sidecar = ALL).
- **uid consistency:** the datasource uid is `loki` (Task 2) and the dashboard logs panel references `uid: loki` (Task 3).
- **Offline verification of infra:** Task 2 Step 5 renders the loki/alloy charts with our values (`helm template`) to catch values-schema errors without a cluster; chart-runtime correctness is confirmed live in Task 5.
- **Out of scope (spec §6):** Tempo/tracing (Plan 15), log alerts, retention/object storage, audit logs, frontend logs.
```
